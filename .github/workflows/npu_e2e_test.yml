# # Tests layout

name: NPU e2e tests

on:
  # Trigger the workflow on push or pull request,
  # but only for the main branch
  push:
    branches:
      - main
      - v0.*
    paths:
      - "**/*.py"
      - .github/workflows/npu_e2e_test.yml
      - "pyproject.toml"
      - "uv.lock"
  pull_request:
    branches:
      - main
    paths:
      - "**/*.py"
      - .github/workflows/npu_e2e_test.yml
      - "pyproject.toml"
      - "uv.lock"

# Cancel jobs on the same ref if a new one is triggered
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

# Declare permissions just read content.
permissions:
  contents: read

jobs:
  lint:
    uses: ./.github/workflows/check_pr_lint.yml

  npu_e2e_tests:
    needs: lint
    if: github.repository_owner == 'ByteDance-Seed'
    name: VeOmni Ascend test (self-host)
    runs-on: [self-hosted, 910b-8]
    timeout-minutes: 60 # Increase this timeout value as needed
    container:
      image: quay.io/ascend/veomni:veomni-8.3.rc2-910b-ubuntu22.04-py3.11-latest
      volumes:
        - /usr/local/dcmi:/usr/local/dcmi
        - /usr/local/bin/npu-smi:/usr/local/bin/npu-smi
        # Whole driver tree so CANN can find all .so's (libc_sec.so, ...).
        - /usr/local/Ascend/driver:/usr/local/Ascend/driver
        - /etc/ascend_install.info:/etc/ascend_install.info:ro
        - /data00/tiger/actions-runner_veomni/proxy/latest_proxy:/tmp/latest_proxy:ro
        - /data00/assets/dataset:/github/home/dataset
        - /data00/assets/models:/github/home/models
      # See npu_unit_tests.yml for the full rationale: `container:` starts a
      # sibling container via the host docker socket, so the runner container's
      # 8-card isolation does NOT carry over. GHA's container.options can't read
      # `runner.*` to emit a per-slice --device list, so we mount all 16 and pin
      # CANN via ASCEND_VISIBLE_DEVICES (derived from $RUNNER_NAME below).
      options: >-
        --device /dev/davinci_manager
        --device /dev/devmm_svm
        --device /dev/hisi_hdc
        --device /dev/davinci0 --device /dev/davinci1 --device /dev/davinci2 --device /dev/davinci3
        --device /dev/davinci4 --device /dev/davinci5 --device /dev/davinci6 --device /dev/davinci7
        --device /dev/davinci8 --device /dev/davinci9 --device /dev/davinci10 --device /dev/davinci11
        --device /dev/davinci12 --device /dev/davinci13 --device /dev/davinci14 --device /dev/davinci15
        --network host
        --shm-size 16g
    env:
      NO_PROXY: "localhost,127.0.0.1,hf-mirror.com"
      HF_ENDPOINT: "https://hf-mirror.com"
      HF_HUB_ENABLE_HF_TRANSFER: "0" # This is more stable
      CI_SAMPLES_DIR: "/data00/assets/samples"
      CI_MODEL_DIR: "/github/home/models/Qwen"
      CI_DATASET_DIR: "/github/home/dataset"

    steps:
      - name: Set ASCEND_VISIBLE_DEVICES from runner slice
        # $RUNNER_NAME is injected by the GitHub Actions runner and equals the
        # `npu_runner_specs[*].name` from ansible (910b-0..5). Even-suffix slices
        # own cards 0-7, odd-suffix own 8-15. See github_runner/README.md.
        run: |
          case "$RUNNER_NAME" in
            910b-*)
              slice_num="${RUNNER_NAME#910b-}"
              if [ "$((slice_num % 2))" -eq 0 ]; then
                cards="0,1,2,3,4,5,6,7"
              else
                cards="8,9,10,11,12,13,14,15"
              fi
              ;;
            *)
              echo "Unexpected RUNNER_NAME=$RUNNER_NAME; falling back to first 8 cards" >&2
              cards="0,1,2,3,4,5,6,7"
              ;;
          esac
          echo "ASCEND_VISIBLE_DEVICES=$cards" >> "$GITHUB_ENV"
          echo "ASCEND_RT_VISIBLE_DEVICES=$cards" >> "$GITHUB_ENV"
          echo "Runner $RUNNER_NAME -> ASCEND_VISIBLE_DEVICES=$cards"
      - name: Set up proxy
        run: |
          CURRENT_PROXY=$(cat /tmp/latest_proxy)
          echo "HTTP_PROXY=$CURRENT_PROXY" >> $GITHUB_ENV
          echo "HTTPS_PROXY=$CURRENT_PROXY" >> $GITHUB_ENV
          echo "http_proxy=$CURRENT_PROXY" >> $GITHUB_ENV
          echo "https_proxy=$CURRENT_PROXY" >> $GITHUB_ENV
      - name: Check npu and CANN info
        run: |
          cat /usr/local/Ascend/ascend-toolkit/latest/"$(uname -i)"-linux/ascend_toolkit_install.info
          npu-smi info
      - name: Check initial pip list from image
        run: |
          pip list
      - name: Checkout ByteDance-Seed/VeOmni repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          clean: true
      - name: Sync dependencies
        run: |
          uv sync --frozen --python 3.11 --extra npu --extra audio --group dev
      - name: Check final pip list
        run: |
          uv pip list
      - name: Run e2e training test
        run: |
          uv run --frozen pytest -v -s -x tests/e2e/test_e2e_parallel.py
      - name: Run FSDP equivalence tests
        run: |
          uv run --frozen pytest -v -s -x tests/distributed/test_fsdp_equivalence.py
      - name: Sync dependencies [transformers-v5]
        run: |
          uv sync --frozen --python 3.11 --no-group transformers-stable --extra transformers5-exp --extra npu --extra audio --group dev
      - name: Run e2e tests [transformers-v5]
        run: |
          uv run --frozen pytest -v -s -x tests/e2e/test_e2e_parallel.py
      - name: Run FSDP equivalence tests [transformers-v5]
        run: |
          uv run --frozen pytest -v -s -x tests/distributed/test_fsdp_equivalence.py

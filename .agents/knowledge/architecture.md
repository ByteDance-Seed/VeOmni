# VeOmni Architecture

The maintained reference is [docs/developer/architecture.md](../../docs/developer/architecture.md).
Read that file when this knowledge entry is requested; it contains the full
module/contract details, not just an overview. Update the public reference when
the implementation changes instead of adding a second copy here.

Agent-specific safety constraints remain in `.agents/knowledge/constraints.md`.

# OmniModel V2 — Qwen3-0.6B master training YAML.
#
# Pure text SFT: token_encode → qwen3_llm → token_decode.
#
# Split the upstream Qwen3 checkpoint first::
#
#   python scripts/convert_model.py \
#     --model_path /mnt/hdfs/veomni/models/transformers/Qwen/Qwen3-0.6B \
#     --output_dir /mnt/hdfs/veomni/models/seed_omni/Qwen3-0.6B
#
# The LLM tokenizer lives in ``qwen3_text_encoder/`` (split checkpoint asset).

modules:
  qwen3_text_encoder:
    model:
      model_path: qwen3_text_encoder

  qwen3_llm:
    model:
      model_path: qwen3_llm

# Flat list of edges; each endpoint is a self-describing `module[.method]`
# string (bare module → `.forward`).  Active nodes are derived from the
# endpoints:  qwen3_text_encoder.encode → qwen3_llm → qwen3_text_encoder.decode → end
training_graph:
  - { from: qwen3_text_encoder.encode, to: qwen3_llm }
  - { from: qwen3_llm,                 to: qwen3_text_encoder.decode }
  - { from: qwen3_text_encoder.decode, to: end }
